<ui:ContentDialog x:Class="ExamSettings.Pages.TimeSlotDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             xmlns:controls="clr-namespace:System.Windows.Controls;assembly=PresentationFramework"
             mc:Ignorable="d"
             Title="时间段编辑"
             PrimaryButtonText="确定"
             SecondaryButtonText="取消"
             PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
             SecondaryButtonClick="ContentDialog_SecondaryButtonClick">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="时间段名称:*" VerticalAlignment="Center" Foreground="Red"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtName" Margin="5,5,0,5" ToolTip="请输入时间段名称"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="开始日期:*" VerticalAlignment="Center" Foreground="Red"/>
        <controls:DatePicker Grid.Row="1" Grid.Column="1" x:Name="dateStart" Margin="5,5,0,5" ToolTip="请选择开始日期"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="开始时间:*" VerticalAlignment="Center" Foreground="Red"/>
        <ui:TimePicker Grid.Row="2" Grid.Column="1" x:Name="timeStart" Margin="5,5,0,5" ToolTip="请选择开始时间"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="结束日期:*" VerticalAlignment="Center" Foreground="Red"/>
        <controls:DatePicker Grid.Row="3" Grid.Column="1" x:Name="dateEnd" Margin="5,5,0,5" ToolTip="请选择结束日期"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="结束时间:*" VerticalAlignment="Center" Foreground="Red"/>
        <ui:TimePicker Grid.Row="4" Grid.Column="1" x:Name="timeEnd" Margin="5,5,0,5" ToolTip="请选择结束时间"/>
    </Grid>
</ui:ContentDialog>